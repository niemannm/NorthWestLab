@model IEnumerable<NorthWestOrderSystem.Models.Wage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WageAmount)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WageAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WageID })
        </td>
    </tr>
}

</table>
